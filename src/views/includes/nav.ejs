<nav class="navbar navbar-expand-md navbar-dark bg-green px-4">
  <div class="container-fluid">

    <!-- Brand com ponto decorativo -->
    <a class="navbar-brand me-5 title d-flex align-items-center gap-2" href="/">
      GeoRecife
    </a>

    <!-- Botão hamburguer -->
    <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu" aria-controls="navbarMenu" aria-expanded="false" aria-label="Abrir menu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Menu -->
    <div class="collapse navbar-collapse" id="navbarMenu">

      <div class="navbar-nav gap-2 ms-md-4 fs-2">

        <!-- Link Login/Sair visível apenas no mobile -->
        <% if (user) { %>
          <a class="nav-link d-md-none nav-white fw-semibold nav-mobile-action" href="/logout">
            <i class="bi bi-box-arrow-right me-1"></i>Sair
          </a>
        <% } else { %>
          <a class="nav-link d-md-none nav-white fw-semibold nav-mobile-action" href="/login">
            <i class="bi bi-person-fill me-1"></i>Login
          </a>
        <% } %>

        <div class="nav-divider d-md-none"></div>

        <a class="nav-link nav-white fw-semibold" href="/">
          <i class="bi bi-house-fill me-1 d-md-none"></i>Início
        </a>
        <a class="nav-link nav-white fw-semibold" href="/mapa">
          <i class="bi bi-map-fill me-1 d-md-none"></i>Mapa
        </a>
        <a class="nav-link nav-white fw-semibold" href="/ocorrencias">
          <i class="bi bi-exclamation-triangle-fill me-1 d-md-none"></i>Incidentes
        </a>
        <a class="nav-link nav-white fw-semibold" href="/dashboard">
          <i class="bi bi-bar-chart-fill me-1 d-md-none"></i>Dashboard
        </a>

      </div>

      <!-- Botão Login/Sair desktop -->
      <% if (user) { %>
        <a href="/logout" class="ms-auto">
          <button class="btn-light ms-auto d-none d-md-block px-5 fw-bold btn-login btn-aument btn-lg fw-bolder">
            Sair
          </button>
        </a>
      <% } else { %>
        <a href="/login" class="ms-auto">
          <button class="btn-light ms-auto d-none d-md-block px-5 fw-bold btn-login btn-aument btn-lg fw-bolder">
            Login
          </button>
        </a>
      <% } %>

    </div>
  </div>
</nav>
