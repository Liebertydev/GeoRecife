<nav class="navbar navbar-expand-md navbar-dark bg-green px-4 py-3">
  <div class="container-fluid">

    <a class="navbar-brand me-5 title" href="/">GeoRecife</a>

    <!-- botão hamburguer -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- menu -->
    <div class="collapse navbar-collapse" id="navbarMenu">

      <div class="navbar-nav gap-3 ms-md-5">
        <!-- Link visível apenas em mobile -->
        <% if (user) { %>
          <a class="nav-link d-md-none nav-white fw-semibold" href="/logout">Sair</a>
        <% } else { %>
          <a class="nav-link d-md-none nav-white fw-semibold" href="/login">Login</a>
        <% } %>

        <a class="nav-link nav-white fw-semibold" href="/">Início</a>
        <a class="nav-link nav-white fw-semibold" href="/mapa">Mapa</a>
        <a class="nav-link nav-white fw-semibold" href="/ocorrencias">Incidentes</a>
      </div>

      <!-- Botão visível apenas em desktop -->
      <% if (user) { %>
        <a href="/logout" class="ms-auto">
          <button class="btn btn-light ms-auto d-none d-md-block px-5 fw-bold btn-login btn-aument btn-lg fw-bolder">
            Sair
          </button>
        </a>
      <% } else { %>
        <a href="/login" class="ms-auto">
          <button class="btn btn-light ms-auto d-none d-md-block px-5 fw-bold btn-login btn-aument btn-lg fw-bolder">
            Login
          </button>
        </a>
      <% } %>

    </div>

  </div>
</nav>